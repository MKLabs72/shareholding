```markdown
# Shareholding Frontend

This is a standard Create-React-App (or whatever base you used) application. It displays the Shareholding UI that interacts with on-chain contracts via ethers.js (or web3.js).

## Setup